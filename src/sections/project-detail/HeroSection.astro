---
import Headline from "@ui/components/Headline.astro";
import Tag from "@ui/components/Tag.astro";

interface Props {
  title?: string;
  tags?: { name: string; value: string }[];
  image?: string;
  imageAlt?: string;
  year?: number;
}

const { title, tags = [], image, imageAlt = "", year } = Astro.props;
---

<section class="pt-10">
  <div class="flex flex-col gap-3">
    <Headline intent="primary">
      {title}
    </Headline>
    <div class="h-[20rem] md:h-[30rem] lg:h-[45.4rem] relative bg-[#D9D9D9]">
      {
        image && (
          <img
            src={image}
            alt={imageAlt}
            class="object-cover max-h-full h-full w-full"
          />
        )
      }
    </div>

    <div class="flex items-center justify-between">
      {
        tags?.length > 0 && (
          <ul class="flex gap-2">
            {tags.map((tag) => (
              <li>
                <Tag intent="primary">{tag?.name}</Tag>
              </li>
            ))}
          </ul>
        )
      }

      <strong>{year}</strong>
    </div>
  </div>
</section>
